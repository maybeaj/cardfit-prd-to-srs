# GitHub Project용 TASK 템플릿

```yaml
name: GitHub Project 용 TASK 템플릿
about: SRS 기반의 구체적인 개발 태스크 명세
title: "[Feature] IN-07: Cron 배치 구성 (일간 스케줄)"
labels: 'feature, part:infra, epic:IN, complexity:M, sprint:S3, priority:p1'
assignees: ''
```

## 🎯 Summary
- 기능명: [IN-07] Cron 배치 구성 (일간 스케줄)
- 목적: 스택에 큐가 없어도 **매일 "대상 조회 → 실행"** 으로 지연 작업을 성립시킨다. 완주 발송 · 최신성 점검 · 만료 판정 · 동의 파기 네 배치가 이 스케줄 위에 선다.

## 🔗 References (Spec & Context)
> 💡 AI Agent & Dev Note: 작업 시작 전 아래 문서를 반드시 먼저 Read/Evaluate 할 것.
- 태스크 리스트: `/docs/[태스크 리스트] CardFit.md` — IN-07 (1.1 인프라 구성)
- SRS 문서: `/docs/[SRS 문서] CardFit (한글).md` §1.5.2 갈래③ · §4.1 REQ-FUNC-010 · §4.2 REQ-NF-004 · REQ-NF-007 · §4.3 REQ-EXC-006
- 픽스처 명세: `/docs/[픽스처 데이터 명세] CardFit (한글).md` §6장 데모 클럭 — 30일 대기를 즉시 재현
- 테스트 명세서(STD): `TC-FUNC-010` · `TC-NF-007` · `TC-EXC-004` · `TC-EXC-006`
- 관련 요구사항: `REQ-FUNC-010` · `REQ-NF-004` · `REQ-NF-007` · `REQ-EXC-006`

## ✅ Task Breakdown (실행 계획)
- [ ] Vercel Cron 구성 — 일간 실행 슬롯 정의와 실행 계정·권한
- [ ] **완주 계측 배치 슬롯** — "선택 +30일 도달 대상 조회 → 발송". 큐 없이 성립한다 (`outcome_logs.status` 가 재발송을 막는다)
- [ ] **약관 최신성 점검 슬롯** — `verified_at` 30일 초과 카드 제외(REQ-NF-007 · `BE-13`)
- [ ] **조합안 만료 판정 슬롯** — 기준일 +30일 경과 · `rule_version` 변경(REQ-EXC-006 · `BE-13`)
- [ ] **동의 철회 파기 슬롯** — 철회 24시간 내 파기(REQ-NF-004 · `BE-02`)
- [ ] **작업 분할 설계** — 수집·점검이 길면 여러 회 실행으로 쪼갠다(§1.5.2 갈래③)
- [ ] 배치 실패 시 알림 — `alerts` 적재(`IN-06`)
- [ ] 재실행 안전성 — 같은 배치를 두 번 돌려도 부작용이 없어야 한다
- [ ] 통합 테스트 — 데모 클럭 시드로 발송 대상 조회, 배치 2회 실행 시 재발송 0건

## 🧪 Acceptance Criteria (BDD/GWT)

**Scenario 1: 30일 대기가 큐 없이 성립한다**
- Given: 데모 클럭 `outcome_seeds` 2건이 적재된 상태에서
- When: 완주 계측 배치를 실행하면
- Then: 두 건이 발송 대상으로 조회되고 `outbox` 에 적재된다(REQ-FUNC-010).

**Scenario 2 (실패 흐름): 배치를 두 번 돌려도 재발송하지 않는다**
- Given: 발송이 이미 기록된 상태에서
- When: 배치를 한 번 더 실행하면
- Then: 재발송이 0건이다. `outcome_logs.status` 전이가 이를 막는다(ADR-04 · GR4).

**Scenario 3 (실패 흐름): 30일 초과 카드가 계산에서 빠진다**
- Given: `verified_at` 이 31일 전인 카드가 있는 상태에서
- When: 최신성 점검 배치를 실행하면
- Then: 해당 카드가 계산 대상에서 제외되고 경고가 적재된다(REQ-NF-007).

**Scenario 4 (실패 흐름): 배치 실패가 조용히 넘어가지 않는다**
- Given: 배치 실행 중 오류가 발생하는 상태에서
- When: 스케줄이 종료되면
- Then: `alerts` 에 실패가 적재된다. 실패한 배치가 성공처럼 보이면 만료·파기가 밀린 채로 남는다.

## ⚙️ Technical & Non-Functional Constraints
- 기술 제약: **C-TEC-007** Vercel Cron으로 트리거만 한다. 별도 큐·워커를 도입하지 않는다
- 성능: 배치가 서버리스 실행 상한을 넘지 않게 쪼갠다. 상한 실측은 `IN-09`가 수행한다
- 보안: 파기 배치는 `audit_logs` 를 건드리지 않는다(REQ-NF-006 · `DA-04`)
- 범위: **이 태스크는 스케줄 배관이다.** 각 배치의 판정 로직은 `BE-02`·`BE-13`·`BE-17`에 있다

## 🏁 Definition of Done (DoD)
- [ ] 모든 Acceptance Criteria를 충족하는가?
- [ ] 대응 테스트 케이스가 구현되고 통과하는가? <!-- STD TC-FUNC-010 · TC-NF-007 · TC-EXC-006 -->
- [ ] 배포 게이트 ①·②를 통과하는가? <!-- C-TEC-007a -->
- [ ] 의존성 경계 린트를 통과하는가? <!-- ADR-02 · IN-04 -->
- [ ] 픽스처 모드에서 외부 호출 0회로 동작하는가?
- [ ] Linter 경고가 없는가?
- [ ] `python3 tools/verify_docs.py` 가 통과하는가?
- [ ] **네 배치 슬롯이 모두 등록되고 재실행 안전한가?**

## 🚧 Dependencies & Blockers
- Depends on: #12 (IN-02)
- Blocks: #50 (BE-02) · #51 (BE-13) · #52 (BE-15) · #53 (BE-17)
- External Blocker: 없음
- ⚠️ **후행 4건** — 배치로 도는 기능 전부가 이 스케줄 뒤에 있다.
